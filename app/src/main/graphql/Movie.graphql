query Movie($filmId: ID) {
    film(id: $filmId) {
        characterConnection {
            characters {
                name
                id
                skinColor
                birthYear
            }
        }
    }
}